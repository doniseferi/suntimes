language: node_js
os: linux
node_js:
  - "lts/*"
jobs:
  include:
    - stage: testing
      after_success:
        - npm run coverage
    - stage: npm release
      before_deploy:
      - npm run build
      deploy:
        provider: npm
        email: doni.seferi@gmail.com
        api_key: $NPM_TOKEN
        skip_cleanup: true
        edge: true
        access: public
        on: 
          branch: master